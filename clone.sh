#!/bin/bash
 echo "Enter the Repository link to clone:"
 read repolink

 getrepo(){
	 echo "Getting Repository Please Wait"
	 git clone $repolink
 }

getrepo#!/bin/bash
 echo "Enter the Repository link to clone:"
 read repolink

 getrepo(){
	 echo "Getting Repository Please Wait"
	 git clone $repolink
 }

getrepo#!/bin/bash
 echo "Enter the Repository link to clone:"
 read repolink

 getrepo(){
	 echo "Getting Repository Please Wait"
	 git clone $repolink
 }

getrepo
